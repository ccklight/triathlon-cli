require "bundler/gem_tasks"
require "rspec/core/rake_task"

require 'pry'
require_relative "./triathlon-cli/version"
require_relative "./triathlon-cli/cli"

RSpec::Core::RakeTask.new(:spec)

task :default => :spec
